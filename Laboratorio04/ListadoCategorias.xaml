<Window x:Class="Laboratorio04.ListadoCategorias"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Laboratorio04"
        mc:Ignorable="d"
Title="Categorías - Neptuno" Height="400" Width="600" Background="#F5F5F7">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Listado de Categorías" FontSize="20" FontWeight="Light" Foreground="#333" Margin="0,0,0,15"/>

        <DataGrid x:Name="dgCategorias" Grid.Row="1" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" Background="White" 
                  BorderBrush="#DDD" RowBackground="White" AlternatingRowBackground="#FAFAFA">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding idcategoria}" Width="50"/>
                <DataGridTextColumn Header="Categoría" Binding="{Binding nombrecategoria}" Width="150"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding descripcion}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
